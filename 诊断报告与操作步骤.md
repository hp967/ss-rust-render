# SS-Rust Render 构建失败诊断报告

生成时间：2026-05-18  
仓库：`hp967/ss-rust-render`  
提交：`052ff5927de732ce740229500c073460d9e562f1`

---

## 📊 日志概览

| 阶段 | 状态 | 说明 |
|------|------|------|
| 克隆仓库 | ⚠️ 警告 | 提示无访问权限，但实际克隆成功 |
| Build（Docker 构建） | ✅ 成功 | #5-#10 全部 DONE |
| 推送镜像 | ✅ 成功 | `Upload succeeded` |
| Deploy（部署） | ❌ 失败 | `Exited with status 128` |

---

## 🔴 核心问题：状态码 128

Render 在部署阶段以 **退出码 128** 结束，原因是：

```
GitHub → Render OAuth 授权令牌失效 / 仓库权限不足
```

- `128` 是 Render 在部署阶段检测到 Git 权限异常时返回的非标准退出码
- 不是 Dockerfile 问题，不是代码问题，**是 GitHub → Render 的 OAuth 连接断开**

---

## 📁 项目文件

```
/home/hp/ss-rust-render/
├── Dockerfile        ← 服务层版本（修复版）
├── render.yaml       ← Render 配置文件
├── README.md         ← 部署说明
└── log.txt           ← Render 构建日志
```

### Dockerfile（当前版本 — 已修复）

```dockerfile
FROM alpine:3.21 AS builder

RUN apk add --no-cache \
      ca-certificates \
      openssl \
      curl \
      xz

RUN update-ca-certificates

ARG SS_VERSION=v1.24.0

RUN curl -fsSL \
      "https://github.com/shadowsocks/shadowsocks-rust/releases/download/${SS_VERSION}/shadowsocks-${SS_VERSION}.x86_64-unknown-linux-musl.tar.xz" \
    | tar -xJ -C /usr/local/bin/ \
    && chmod +x /usr/local/bin/*

FROM scratch

COPY --from=builder /usr/local/bin /usr/local/bin
COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/

ENV PATH="/usr/local/bin:${PATH}"

EXPOSE ${SS_SERVER_PORT:-8388}

CMD ["/usr/local/bin/shadowsocks", \
     "server", \
     "-u", \
     "-p", "${SS_SERVER_PORT:-8388}", \
     "-k", "${SS_PASSWORD}", \
     "-m", "${SS_METHOD:-aes-256-gcm}"]
```

### render.yaml（当前配置）

```yaml
name: ss-rust-proxy
type: web

envVars:
  - key: SS_SERVER_PORT
    value: "8388"
  - key: SS_PASSWORD
    value: "YOUR_SECRET_PASSWORD_HERE"       # ← 部署后在 Dashboard 改为 Secret
  - key: SS_METHOD
    value: aes-256-gcm

resources:
  cpu: 0.1
  memory: 512MB

autoDeploy: true
```

---

## 🔧 修复步骤

### 方法 A：在服务页面重新授权（推荐）

1. 登录 https://dashboard.render.com
2. 左侧菜单 → 找到你的服务 `ss-rust-proxy`
3. 顶部标签页 → **Settings**（或 **Connect Repo**）
4. 找到 **Connected Repository** 区块
5. 右侧显示 `github.com/hp967/ss-rust-render` → 点击 **Disconnect**
6. 弹出确认 → 点击 **Reconnect**
7. GitHub 登录 → 勾选 `hp967` 账号下的所有仓库权限
8. 点击 **Authorize**
9. Render → 服务页 → **Manual Deploy** → **Deploy latest commit**

### 方法 B：全局 GitHub 账户重连

1. 左侧菜单最底部 → **Account** → **`GitHub Account`**
   - 或右上角头像下拉 → **Account Settings** → GitHub
2. 点击 **Disconnect from GitHub**
3. 点击 **Connect GitHub Account**
4. GitHub 授权弹窗 → 勾选**所有仓库权限**（含私有仓库）
5. 点击 **Authorize and Install Render on your GitHub account**
6. 等待授权完成 → 回到 `ss-rust-proxy` → **Manual Deploy**

### 方法 C：GitHub App 重装（界面找不到 Reconnect 时用）

浏览器打开：

```
https://github.com/apps/render
```

→ **Install in your account** → 勾选 `hp967` → **Submit**

---

## ✅ 操作后验证

```bash
# GitHub 侧：确认 Token 可访问仓库
curl -s -H "Authorization: token YOUR_TOKEN" \
     https://api.github.com/repos/hp967/ss-rust-render | \
     jq '.name, .private'

# 预期输出：
# "ss-rust-render"
# true

# Render 服务状态：State = Live / Running
curl -s https://ss-rust-proxy.onrender.com 2>&1 | head -5
```

---

## ⚠️ 须知

1. **GitHub OAuth Token 有效期约 60 天**，到期后 Render 会再次断开
2. 私有仓库推荐长期使用 **Git Deploy Key**（更稳定，权限更小）
3. `render.yaml` 中的 `SS_PASSWORD` 目前是占位符，**部署后不要提交真实密码**，应在 Render Dashboard 的 **Environment** 标签页中改为 Secret 类型
4. 密码修改后 Render 会自动重启服务

---

## 📌 一句话总结

> Dockerfile 和 render.yaml **没有任何问题**。  
> `Exited with status 128` 100% 是 GitHub → Render 的 OAuth 授权失效。  
> 只需在 Render Dashboard 重新授权 GitHub 私有仓库权限后触发手动部署即可。
